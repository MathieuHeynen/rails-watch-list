<div class="container mt-3">
  <div>
    <h1 class="text-center"><%= @list.name %></h1>
    <%= link_to '🗑️', list_path(@list[:id]), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: 'btn btn-outline-danger' %>
    <%= link_to 'Create a new bookmark', new_list_bookmark_path(@list), class: 'btn btn-outline-secondary' %>
    <% @bookmarks.each do |bookmark| %>
    <div>
      <li><%= movie.title %></li>
      <%= link_to '🗑️', bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: 'btn btn-outline-danger' %>
      <% end %>
    </div>
  </div>
</div>
